document.addEventListener('DOMContentLoaded', () => {
    // TODO: fetch GET `${API_BASE}/api/eventos/{id}/estadisticas`, poblar
    //       #total-inscritos, #total-asistentes, #porcentaje-asistencia, y
    //       renderizar Chart.js en #grafico-inscripciones-por-dia y
    //       #grafico-asistencia-por-hora.
});
