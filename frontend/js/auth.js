document.addEventListener('DOMContentLoaded', () => {
    // TODO: auth/login.html -> #form-login -> fetch POST `${API_BASE}/api/auth/login`
    //       con { credentials: 'include' } para que la cookie de sesión se guarde.
    // TODO: auth/registro.html -> #form-registro -> fetch POST `${API_BASE}/api/auth/registro`.
});
