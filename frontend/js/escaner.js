document.addEventListener('DOMContentLoaded', () => {
    // TODO: inicializar Html5Qrcode sobre #lector-qr; al detectar un código,
    //       fetch POST `${API_BASE}/api/asistencia/validar` con
    //       { tokenEscaneado } y credentials: 'include'; mostrar el resultado
    //       en #resultado-escaneo.
});
