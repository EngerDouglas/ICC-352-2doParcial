# Diagrama de clases

Coloca aquí el diagrama de clases del proyecto (requerimiento #9 del PDF), en
formato `.png`, `.pdf`, `.drawio` o `.puml`.

El modelo de dominio actual (`src/main/java/edu/pucmm/eventosacademicos/modelo/`)
consta de: `Usuario`, `Evento`, `Inscripcion`, `Asistencia`, y los enums
`RolUsuario`, `EstadoEvento`, `EstadoInscripcion`.
